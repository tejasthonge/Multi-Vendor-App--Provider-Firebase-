import 'package:flutter/material.dart';

class VendorEarnigsScreen extends StatelessWidget {
  const VendorEarnigsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold( 

      body: Center(
        child: Text('Earnigs'),
      ),
    );
  }
}